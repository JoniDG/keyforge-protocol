# Subagents — keyforge-protocol

Project-scoped subagents go here. One file per agent, with frontmatter:

```markdown
---
name: agent-name
description: When to use this agent
tools: Read, Grep, Bash(make *)
---

System prompt for the agent.
```

Empty for now — add agents when a recurring research/review task warrants its own context window.
