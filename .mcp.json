{
  "$comment": "MCP servers for keyforge-protocol. Commited so the team shares the same tooling.",
  "mcpServers": {
    "context7": {
      "$comment": "Up-to-date library docs (json-schema, generators). Avoids API hallucinations.",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    }
  }
}
